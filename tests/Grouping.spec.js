import { test, expect } from '@playwright/test';

test.describe('Grouping Login Test', () => {

    test('Valid Login', () => {
        console.log("Valid Login");
    })

    test('Invalid Login', () => {
        console.log("Invalid Login");
    })

})